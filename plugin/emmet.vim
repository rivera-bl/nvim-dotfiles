
"let g:user_emmet_mode       = 'n'   "Only available on normal mode
let g:user_emmet_leader_key = ','

" this two lines are commented because I can't seem to be able to add support for only html, css and js. so now it works in any file
" let g:user_emmet_install_global = 0 "Enable only for html & css 
" autocmd FileType html,css EmmetInstall
